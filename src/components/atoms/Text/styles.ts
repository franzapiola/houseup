import {Colors} from '@globalStyles';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  textBase: {
    fontSize: 30,
    color: Colors.dark,
  },
  title: {
    fontSize: 40,
  },
  body: {
    fontSize: 20,
  },
});
