import { Colors } from '@globalStyles';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  divider: {
    width: '100%',
    height: 2,
    borderRadius: 1,
    marginVertical: 10,
  },
});
